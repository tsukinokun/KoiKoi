using UnityEngine;

public class ClickTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void OnMouseDown()
    {
        Debug.Log("Simple Click Success!");
    }
}
